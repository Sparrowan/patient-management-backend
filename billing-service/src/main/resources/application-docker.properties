# Container/production profile. Native structured JSON logging (Boot 3.4+, ECS format); MDC
# (requestId) is included automatically — no external logstash-encoder dependency needed.
logging.structured.format.console=ecs
