# Container/production profile.
# Native structured logging (Spring Boot 3.4+): one JSON object per line in Elastic Common
# Schema, ready for Loki/ELK/CloudWatch. MDC (requestId) is included automatically, so no
# external logstash-encoder dependency is needed.
logging.structured.format.console=ecs
